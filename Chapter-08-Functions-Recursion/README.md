# 📂 Chapter 08 - Functions & Recursions

<div align="center">

[![Files](https://img.shields.io/badge/Files-0-blue?style=for-the-badge)](#)

---

This chapter covers modularizing code using custom function definitions, passing parameters, assigning default argument values, and solving problems using recursive self-calls.

</div>

## 📓 Core Topics Covered
* **Functions**: Named, reusable blocks of code that perform specific tasks.
  * *Function Definition*: Creating a function using the `def` keyword.
  * *Function Call*: Executing the function using its name followed by parentheses.
* **Types of Functions**:
  * *Built-in Functions*: Pre-defined in Python (e.g. `print()`, `len()`, `range()`).
  * *User-defined Functions*: Created by the programmer to perform custom tasks.
* **Arguments and Parameters**: Values passed into a function during execution.
* **Default Parameter Value**: Assigning standard fallback values to parameters (used when arguments are omitted in the function call).
* **Recursion**: A programming technique where a function calls itself directly or indirectly to solve a smaller instance of the same problem.

---

## 📝 Practice Exercises (Planned)
* [ ] Find the greatest of three numbers using a function
* [ ] Convert Celsius to Fahrenheit using a function
* [ ] Prevent `print()` from generating a new line at the end
* [ ] Calculate sum of first n natural numbers using recursion
* [ ] Print first n lines of a star pattern using recursion
* [ ] Convert inches to cms using a function
* [ ] Remove a word from a list and strip spaces using functions
* [ ] Print multiplication table of a given number using functions
